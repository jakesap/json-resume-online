services:
  - type: web
    name: json-resume-online
    runtime: static
    buildCommand: bun install && bun run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
